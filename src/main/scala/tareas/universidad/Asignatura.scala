package tareas.universidad

class Asignatura(nom:string,codigo:String,descrip:String,
                  departamento:Departamento)
                  {
  var asignaturas=new Asignatura

  Asignaturas.Agregar(asignatura)
  if (asignatura.esDelaCarrera(carrera))
}
